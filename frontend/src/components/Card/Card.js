import React from "react";

export default function Card() {
	return (
		<div className={classes.card}>
			<span>HELLO I AM A CARD</span>
			<button className={classes.button}>CLICK ME</button>
		</div>
	);
}
