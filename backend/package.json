{
	"name": "news-archiver-backend",
	"version": "1.0.0",
	"description": "The backend code for the news archiver project. The code runs a CRON job every 15 minutes to scrape the articles of the newest headlines from both fox and cnn websites",
	"main": "server.js",
	"scripts": {
		"test": "echo \"Error: no test specified\" && exit 1",
		"start": "node server",
		"dev": "cross-env NODE_ENV=development nodemon server"
	},
	"keywords": [],
	"author": "lanxion",
	"license": "ISC",
	"dependencies": {
		"aws-sdk": "^2.761.0",
		"body-parser": "^1.19.0",
		"cors": "^2.8.5",
		"dot-env": "0.0.1",
		"express": "^4.17.1",
		"mongodb": "^3.6.2",
		"mongoose": "^5.10.7",
		"validator": "^13.1.17"
	},
	"devDependencies": {
		"cross-env": "^7.0.2",
		"nodemon": "^2.0.4"
	}
}
